---
apiVersion: batch/v1
kind: CronJob
metadata:
  name: awpa2
  namespace: projects
spec:
  schedule: "0 7,22 * * *"  # Every day at 7am and 10pm
  jobTemplate:
    spec:
      template:
        spec:
          restartPolicy: Never
          containers:
            - name: main
              image: maneeshwije/awpa:latest
              env:
                - name: SENDER_EMAIL
                  valueFrom:
                    secretKeyRef:
                      name: awpa2-secrets
                      key: SENDER_EMAIL
                - name: RECEIVER_EMAIL
                  valueFrom:
                    secretKeyRef:
                      name: awpa2-secrets
                      key: RECEIVER_EMAIL
                - name: SENDER_PASS
                  valueFrom:
                    secretKeyRef:
                      name: awpa2-secrets
                      key: SENDER_PASS
                - name: URL
                  valueFrom:
                    secretKeyRef:
                      name: awpa2-secrets
                      key: URL
                - name: DELTA
                  valueFrom:
                    secretKeyRef:
                      name: awpa2-secrets
                      key: DELTA
                - name: DATA_PATH
                  valueFrom:
                    secretKeyRef:
                      name: awpa2-secrets
                      key: DATA_PATH
              volumeMounts:
                - name: amazon-session
                  mountPath: /app/amazon.json
                  subPath: amazon.json

                - name: data
                  mountPath: /data
          volumes:
            - name: amazon-session
              secret:
                secretName: awpa2-secrets
            - name: data
              persistentVolumeClaim:
                claimName: awpa2-data
