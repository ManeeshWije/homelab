---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: wireguard
  namespace: admin
spec:
  interval: 5m
  upgrade:
    force: true
  chart:
    spec:
      chart: wg-easy
      version: 17.0.0
      sourceRef:
        kind: HelmRepository
        name: truecharts
        namespace: flux-system
      interval: 5m
  values:
    image:
      repository: ghcr.io/wg-easy/wg-easy
      pullPolicy: IfNotPresent
      tag: 15@sha256:07ed8f95eeeb4082c2e6afe3f5f3991723f496a21dd2dc25860b119a728b9ab0
    securityContext:
      container:
        PUID: 0
        runAsUser: 0
        runAsGroup: 0
        readOnlyRootFilesystem: false
        runAsNonRoot: false
        privileged: true
        capabilities:
          add:
            - NET_ADMIN
            - NET_RAW
            - SYS_MODULE
    service:
      vpn:
        loadBalancerIP: ${wg_host}
        type: LoadBalancer
        externalTrafficPolicy: Local
    workload:
      main:
        podSpec:
          nodeSelector:
            kubernetes.io/arch: arm64
          containers:
            main:
              env:
                WG_HOST: "${wg_host}"
                WG_MTU: 1280
                WG_PERSISTENT_KEEPALIVE: 0
                WG_DEFAULT_DNS: "${traefik_load_balancer_ip}"
                WG_ALLOWED_IPS: "0.0.0.0/0, ::/0"
                PASSWORD_HASH: "${wg_password_hash}"
                LANG: "en"
                UI_TRAFFIC_STATS: "false"
    persistence:
      config:
        enabled: true
        mountPath: "/etc/wireguard"
        existingClaim: wireguard-config
